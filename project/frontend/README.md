# Frontend (React)

В этой папке находится клиентское приложение на React.

Функционал MVP:

- форма ввода параметров заявки;
- вызов `POST /predict` backend API;
- отображение решения, вероятности дефолта и причин.

Frontend должен быть изолирован от ML-логики и работать через HTTP API.
