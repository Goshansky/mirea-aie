# Артефакты ML

Папка для сохранения результатов обучения:

- сериализованные модели (`.joblib`);
- сохранённый препроцессор;
- метрики и служебные файлы с признаками;
- дополнительные файлы explainability (при необходимости).

Backend должен читать артефакты только из этой папки.
